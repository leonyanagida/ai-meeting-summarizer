# Hugging Face API Key
# Get your API key from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# Redis Configuration (Upstash)
# Option 1 (Recommended): Add directly through Vercel
# 1. Go to your Vercel project
# 2. Click "Storage"
# 3. Select "Connect Store" -> "Upstash Redis"
# 4. Follow the prompts to create/connect your database
# Vercel will automatically add REDIS_URL and REDIS_TOKEN to your environment variables

# Option 2 (Manual): Get from Upstash Console
# 1. Go to https://console.upstash.com/
# 2. Create a new database
# 3. In your database details, find these values:
# - REDIS_URL: Copy the "REST URL" field
# - REDIS_TOKEN: Copy the "REST Token" field
REDIS_URL=your_upstash_redis_rest_url_here
REDIS_TOKEN=your_upstash_redis_rest_token_here

# Optional: Model ID (if you want to use a different model)
# Default: philschmid/bart-large-cnn-samsum
# HUGGINGFACE_MODEL_ID=your_preferred_model_id